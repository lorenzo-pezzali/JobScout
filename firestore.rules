rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the Cloud Function (Admin SDK, bypasses rules).
    // No client ever talks to Firestore directly, so lock it down completely.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
